#ifndef CLAPTRAP_H
#define CLAPTRAP_H

class ClapTrap {
	public:
		// Default constructor
		ClapTrap();

		// Copy constructor
		ClapTrap(const ClapTrap &other);

		// Copy assignment operator
		ClapTrap& operator=(const ClapTrap &other);

		// Destructor
		~ClapTrap();

	private:
		// Class members
};

#endif // CLAPTRAP_H
